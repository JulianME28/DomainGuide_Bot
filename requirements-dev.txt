# Залежності для розробки (лінтер і тести).
# Встановити:  .venv\Scripts\python.exe -m pip install -r requirements-dev.txt

-r requirements.txt

ruff>=0.8                 # перевірка стилю коду + автоформатування
pytest>=8.3               # запуск тестів
pytest-asyncio>=0.24      # підтримка асинхронних тестів
